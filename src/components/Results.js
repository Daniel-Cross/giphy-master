import React from 'react';
import PropTypes from 'prop-types';
import Result from './Result';

const Results = (props) => {
	return (
		<div id="Results">
			<p>hello results page</p>
			<Result />
		</div>
	);
};

export default Results;
